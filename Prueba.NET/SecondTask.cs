/*
 * In the army, each soldier has an assigned rank. A soldier of rank X has to
 * report to (any) soldier of rank X+1. Many Soldiers can report to the same
 * superior. Examples:
 *      + ranks = [3,4,3,0,2,2,3,0,0] should return 5
 *      + ranks = [4,2,0] should return 0
 */

using System;


namespace Prueba.NET
{
    public class SecondTask
    {
        /// TODO: Write a function int Solutions(int[] ranks) that given an
        /// array ranks consisting ranks return the number of soldiers who can
        /// report to some Superior. 
        public int Solution(int[] ranks)
        {
            return 0;
        }
    }
}
